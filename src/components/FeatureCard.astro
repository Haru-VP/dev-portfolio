---
const { title, description, color, icon } = Astro.props
---

<div
  class="group bg-card/70 backdrop-blur border border-pastel-pink/20 rounded-2xl p-8
         transition-all hover:-translate-y-2 hover:shadow-xl"
  style={`box-shadow: 0 10px 40px rgba(244, 167, 193, 0.12);`}
>
  <div
    class="mb-4 text-4xl"
    style={`color: ${color}`}
  >
    {icon}
  </div>

  <h3 class="font-serif text-2xl font-semibold mb-3 text-gray-800">
    {title}
  </h3>

  <p class="text-gray-600 leading-relaxed">
    {description}
  </p>
</div>
